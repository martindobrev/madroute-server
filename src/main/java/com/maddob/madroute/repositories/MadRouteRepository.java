package com.maddob.madroute.repositories;

import com.maddob.madroute.domain.MadRoute;
import org.springframework.data.repository.CrudRepository;

public interface MadRouteRepository extends CrudRepository<MadRoute, Long> {
}
